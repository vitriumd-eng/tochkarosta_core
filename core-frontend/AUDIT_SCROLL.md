# Аудит кода скролла

**Дата:** 2025-01-21  
**Статус:** ❌ КРИТИЧЕСКИЕ ПРОБЛЕМЫ ОБНАРУЖЕНЫ

---

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1. Конфликт CSS `scroll-behavior: smooth` с JS-анимацией

**Файлы:**
- `core-frontend/components/platform/Hero.tsx` (строки 35-75)
- `core-frontend/components/platform/VideoBox.tsx` (строки 35-75)
- `core-frontend/app/globals.css` (строки 15, 34, 311, 318)

**Проблема:**
В `Hero.tsx` и `VideoBox.tsx` используется JS-анимация скролла через `requestAnimationFrame`, но **НЕ отключается** CSS `scroll-behavior: smooth`. Это создает конфликт:

- JS говорит: "Скролль на пиксель 10"
- Браузер: "Окей, начну плавно ехать к 10... это займет 0.5 сек"
- Через 16мс JS: "Скролль на пиксель 20"
- Браузер: "Отменяю поездку к 10, начинаю плавно ехать к 20"

**Результат:** Скролл "залипает" или не работает плавно.

**Решение:**
Добавить отключение CSS `scroll-behavior` перед анимацией (как в финальной версии из `2.txt`):

```typescript
// === FIX 1: Отключаем CSS Smooth Scroll ===
const originalScrollBehavior = document.documentElement.style.scrollBehavior
document.documentElement.style.scrollBehavior = 'auto'

// ... код анимации ...

// === FIX 1 (End): Возвращаем CSS Smooth Scroll ===
document.documentElement.style.scrollBehavior = originalScrollBehavior || ''
```

---

### 2. Устаревшая формула расчета дистанции

**Файлы:**
- `core-frontend/components/platform/Hero.tsx` (строка 45)
- `core-frontend/components/platform/VideoBox.tsx` (строка 45)

**Проблема:**
Используется старая формула:
```typescript
const targetPos = targetElement.getBoundingClientRect().top + startPos - windowHeight / 2 + targetElement.clientHeight / 2
const distance = targetPos - startPos
```

**Решение:**
Использовать исправленную формулу из `2.txt`:
```typescript
const targetRect = targetElement.getBoundingClientRect()
const containerRect = { top: 0, height: window.innerHeight }
const relativeTargetTop = targetRect.top - containerRect.top
const distance = relativeTargetTop + (targetElement.clientHeight / 2) - (containerRect.height / 2)
```

---

## ⚠️ СРЕДНИЕ ПРОБЛЕМЫ

### 3. CSS `scroll-behavior: smooth` в глобальных стилях

**Файл:** `core-frontend/app/globals.css`

**Проблема:**
- Строка 15: `scroll-behavior: smooth;` на `html`
- Строка 34: `scroll-behavior: smooth;` в `@supports`
- Строка 311: `.scroll-smooth { scroll-behavior: smooth; }`
- Строка 318: `.scroll-smooth { scroll-behavior: smooth; }` в `@supports`

**Влияние:**
Эти стили конфликтуют с JS-анимацией скролла в `Hero.tsx` и `VideoBox.tsx`.

**Решение:**
1. Удалить `scroll-behavior: smooth` из `html` (строка 15)
2. Оставить только для элементов, где нужен нативный smooth scroll (не JS-анимация)
3. Или использовать JS-отключение (см. Критическая проблема #1)

---

### 4. `scroll-snap-type` может конфликтовать

**Файл:** `core-frontend/app/globals.css` (строка 16)

**Проблема:**
```css
scroll-snap-type: y mandatory;
```

**Влияние:**
`scroll-snap-type` может конфликтовать с JS-анимацией скролла, создавая "дергание" или непредсказуемое поведение.

**Решение:**
Удалить или закомментировать, если не используется активно.

---

### 5. Класс `scroll-smooth` в компонентах

**Файл:** `core-frontend/components/platform/StatsFeaturesBridge.tsx` (строка 305)

**Проблема:**
```tsx
className="space-y-4 overflow-y-auto scroll-smooth transition-opacity duration-300 flex-1"
```

**Влияние:**
Если в этом компоненте используется JS-анимация скролла, класс `scroll-smooth` будет конфликтовать.

**Решение:**
Удалить `scroll-smooth` из className, если используется JS-анимация.

---

## ✅ ПОЛОЖИТЕЛЬНЫЕ МОМЕНТЫ

1. ✅ Используется правильная easing-функция `easeInOutQuad` из `2.txt`
2. ✅ `startTime` устанавливается внутри `animation` функции (правильно)
3. ✅ Используется `IntersectionObserver` для определения момента скролла
4. ✅ Есть защита от повторных запусков через `isScrollingRef`
5. ✅ Правильная очистка в `useEffect` cleanup

---

## 📋 ПЛАН ИСПРАВЛЕНИЙ

### Приоритет 1 (Критично):
1. ✅ **ВЫПОЛНЕНО** - Добавить отключение CSS `scroll-behavior` в `Hero.tsx`
2. ✅ **ВЫПОЛНЕНО** - Добавить отключение CSS `scroll-behavior` в `VideoBox.tsx`
3. ✅ **ВЫПОЛНЕНО** - Обновить формулу расчета дистанции в обоих файлах

**Дата исправления:** 2025-01-21

### Приоритет 2 (Важно):
4. ⚠️ Удалить `scroll-behavior: smooth` из `html` в `globals.css` (или оставить, но с JS-отключением)
5. ⚠️ Удалить `scroll-snap-type: y mandatory` из `globals.css` (если не используется)

### Приоритет 3 (Опционально):
6. ⚠️ Проверить использование `scroll-smooth` в `StatsFeaturesBridge.tsx`

---

## 📝 РЕКОМЕНДАЦИИ

1. **Создать утилиту** `smoothScrollTo` в отдельном файле (например, `utils/smoothScroll.ts`) и использовать её в `Hero.tsx` и `VideoBox.tsx` вместо дублирования кода.

2. **Документировать** все места, где используется JS-анимация скролла, чтобы избежать конфликтов в будущем.

3. **Тестировать** на разных браузерах после исправлений, особенно Safari и Firefox.

---

## 🔗 ССЫЛКИ

- Финальная версия кода: `core-frontend/utils/smoothScroll.md`
- Исходный гайд: `2.txt` (строки 182-287)

