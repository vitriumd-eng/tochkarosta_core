# Детальный аудит скролла

**Дата:** 2025-01-21  
**Статус:** 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ ОБНАРУЖЕНЫ

---

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1. Неправильное условие срабатывания для Hero блока

**Файл:** `core-frontend/components/platform/Hero.tsx` (строка 23)

**Проблема:**
```typescript
const isBottomJustVisible = rect.bottom > 0 && rect.bottom < 100 && rect.top < 0
```

**Анализ:**
- Условие требует, чтобы `rect.bottom` был между 0 и 100px от верха экрана
- И одновременно `rect.top < 0` (верх блока выше экрана)
- Это означает, что блок должен быть очень высоким или находиться в очень специфической позиции
- Для блока `min-h-screen` (100vh) это условие может не срабатывать при скролле вверх

**Правильная логика (по аналогии с VideoBox):**
- VideoBox срабатывает когда: `rect.top < windowHeight && rect.top > windowHeight - 100` (верх блока только появился снизу)
- Hero должен срабатывать когда: `rect.bottom > 0 && rect.bottom < 100` (нижняя часть только появилась сверху)

**Решение:**
Изменить условие на:
```typescript
const isBottomJustVisible = rect.bottom > 0 && rect.bottom < 100
```

---

### 2. Конфликт CSS `scroll-snap-type` с JS-анимацией

**Файл:** `core-frontend/app/globals.css` (строка 16)

**Проблема:**
```css
scroll-snap-type: y mandatory;
```

**Влияние:**
- `scroll-snap-type: y mandatory` принудительно "прилипает" к секциям при скролле
- Это конфликтует с JS-анимацией скролла в `Hero.tsx` и `VideoBox.tsx`
- Браузер может переопределять позицию скролла, создавая "дергание"

**Решение:**
Удалить или закомментировать `scroll-snap-type: y mandatory` из `globals.css`

---

### 3. Конфликт CSS `scroll-behavior: smooth` (частично решено)

**Файл:** `core-frontend/app/globals.css` (строка 15)

**Статус:** ✅ Частично решено через JS-отключение, но лучше удалить из CSS

**Проблема:**
- CSS `scroll-behavior: smooth` установлен глобально
- Хотя JS-код временно отключает его, лучше убрать из CSS для избежания конфликтов

---

### 4. Неправильная логика определения "нижней части" для Hero

**Файл:** `core-frontend/components/platform/Hero.tsx` (строка 23)

**Проблема:**
Текущее условие `rect.top < 0` означает, что верх блока должен быть выше экрана. Но для блока `min-h-screen` это может быть неверно.

**Правильная логика:**
При скролле вверх, когда мы находимся ниже Hero блока, нижняя часть Hero появляется в viewport. Условие должно быть:
- `rect.bottom > 0` - нижняя часть видна
- `rect.bottom < 100` - нижняя часть только появилась (в пределах 100px от верха экрана)
- `rect.top < windowHeight` - верх блока еще виден или только что скрылся

---

## ⚠️ СРЕДНИЕ ПРОБЛЕМЫ

### 5. Отсутствие проверки направления скролла

**Файлы:** `Hero.tsx`, `VideoBox.tsx`

**Проблема:**
- Hero должен срабатывать только при скролле вверх
- VideoBox должен срабатывать только при скролле вниз
- Сейчас нет явной проверки направления

**Решение:**
Добавить отслеживание направления скролла через `lastScrollY` ref

---

### 6. Возможный конфликт между двумя автоскроллами

**Проблема:**
- Если Hero и VideoBox находятся близко друг к другу, оба могут пытаться центрировать одновременно
- Флаг `isScrollingRef` не синхронизирован между компонентами

**Решение:**
Использовать глобальный флаг или более точные условия срабатывания

---

## ✅ ПОЛОЖИТЕЛЬНЫЕ МОМЕНТЫ

1. ✅ Правильная реализация отключения CSS `scroll-behavior` в JS
2. ✅ Правильная формула расчета дистанции (из 2.txt)
3. ✅ Правильная easing-функция `easeInOutQuad`
4. ✅ Защита от повторных запусков через `isScrollingRef`

---

## 📋 ПЛАН ИСПРАВЛЕНИЙ

### Приоритет 1 (Критично):
1. ✅ Исправить условие `isBottomJustVisible` в Hero.tsx
2. ✅ Удалить `scroll-snap-type: y mandatory` из globals.css
3. ✅ Улучшить логику определения появления нижней части Hero

### Приоритет 2 (Важно):
4. ⚠️ Добавить проверку направления скролла для Hero (только вверх)
5. ⚠️ Добавить проверку направления скролла для VideoBox (только вниз)

### Приоритет 3 (Опционально):
6. ⚠️ Удалить `scroll-behavior: smooth` из CSS (оставить только JS-контроль)

---

## 🔍 ДЕТАЛЬНОЕ СРАВНЕНИЕ

### VideoBox (работает):
```typescript
// Когда верхняя часть VideoBox только появляется в viewport (при скролле вниз)
const isTopJustVisible = rect.top < windowHeight && rect.top > windowHeight - 100
```
- `rect.top < windowHeight` - верх блока виден или только что появился
- `rect.top > windowHeight - 100` - верх блока в пределах 100px от низа экрана
- **Результат:** Срабатывает когда верх блока только появился снизу

### Hero (НЕ работает):
```typescript
// Когда нижняя часть Hero только появляется при скролле вверх
const isBottomJustVisible = rect.bottom > 0 && rect.bottom < 100 && rect.top < 0
```
- `rect.bottom > 0` - нижняя часть видна
- `rect.bottom < 100` - нижняя часть в пределах 100px от верха экрана
- `rect.top < 0` - **ПРОБЛЕМА:** верх блока должен быть выше экрана (неверно для min-h-screen)

**Правильное условие для Hero:**
```typescript
const isBottomJustVisible = rect.bottom > 0 && rect.bottom < 100
```
Убрать `&& rect.top < 0` - это условие слишком строгое и неверное для блока высотой 100vh.

---

## 🎯 РЕКОМЕНДАЦИИ

1. **Упростить условие Hero** - убрать проверку `rect.top < 0`
2. **Удалить scroll-snap** - он конфликтует с JS-анимацией
3. **Добавить логирование** для отладки (временно)
4. **Тестировать на разных высотах экрана**


