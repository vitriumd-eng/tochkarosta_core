# Аудит прокрутки - Найденные проблемы

## 🔴 Критические проблемы

### 1. Конфликт двух систем прокрутки
**Проблема:** Две независимые системы прокрутки работают одновременно:
- `HeroMedia.tsx` - автоматическая прокрутка при загрузке (3 секунды)
- `FloatingSphere.tsx` - `autoScrollToCenter()` при скролле (behavior: 'smooth')

**Последствия:**
- Дергание прокрутки
- Конфликт анимаций
- Неконтролируемое поведение

### 2. Множественные вызовы getBoundingClientRect
**Проблема:** `getBoundingClientRect()` вызывается во время анимации прокрутки
- В `HeroMedia.tsx` - при каждом кадре анимации
- В `FloatingSphere.tsx` - при каждом событии scroll

**Последствия:**
- Принудительный reflow
- Падение производительности
- Дергание анимации

### 3. Отсутствие защиты от конфликтов
**Проблема:** Нет механизма предотвращения одновременной работы нескольких прокруток
- `isAutoScrolling` в FloatingSphere не синхронизирован с HeroMedia
- Нет глобального флага прокрутки

### 4. Неоптимальное использование scrollTo
**Проблема:** 
- `window.scrollTo({ behavior: 'auto' })` в цикле requestAnimationFrame
- `window.scrollTo({ behavior: 'smooth' })` в FloatingSphere
- Два разных подхода конфликтуют

## ✅ Решения (ВЫПОЛНЕНО)

1. ✅ Добавлен глобальный флаг прокрутки (`window.__isGlobalScrolling`)
2. ✅ Отключен autoScrollToCenter во время прокрутки HeroMedia
3. ✅ Кэширован getBoundingClientRect (вызывается один раз, не в цикле)
4. ✅ Оптимизирован scrollTo (используется напрямую без объекта)
5. ✅ Добавлены флаги для предотвращения множественных вызовов

## 🔧 Внесенные изменения

### HeroMedia.tsx:
- Добавлен `isScrollingRef` для отслеживания состояния прокрутки
- Кэширование `getBoundingClientRect()` - вызывается один раз перед анимацией
- Глобальный флаг `window.__isGlobalScrolling` для синхронизации с FloatingSphere
- Оптимизирован `window.scrollTo(0, currentScroll)` вместо объекта
- Увеличена задержка старта до 500ms для стабильности

### FloatingSphere.tsx:
- Добавлена проверка `window.__isGlobalScrolling` перед autoScrollToCenter
- Пропуск автоматической прокрутки во время глобальной прокрутки HeroMedia

## 📊 Результаты

- ✅ Устранены конфликты между системами прокрутки
- ✅ Оптимизирована производительность (меньше reflow)
- ✅ Плавная прокрутка без дергания
- ✅ Правильная синхронизация между компонентами

