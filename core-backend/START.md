# ИНСТРУКЦИЯ ПО ЗАПУСКУ

**Дата:** 2025-01-16  
**Статус:** ✅ Проект готов к запуску

---

## БЫСТРЫЙ СТАРТ

### 1. Активировать виртуальное окружение

```bash
cd core-backend
venv\Scripts\activate  # Windows
# source venv/bin/activate  # Linux/macOS
```

### 2. Убедиться, что зависимости установлены

```bash
pip install -r requirements.txt
```

### 3. Настроить базу данных

Откройте `core-backend/.env` и установите правильный `DATABASE_URL`:

```env
DATABASE_URL=postgresql+asyncpg://postgres:ВАШ_ПАРОЛЬ@localhost:5432/modular_saas_core
```

**Важно:** Замените `ВАШ_ПАРОЛЬ` на реальный пароль PostgreSQL.

См. `SETUP_DATABASE.md` для подробных инструкций.

### 4. Применить миграции (если еще не применены)

```bash
alembic upgrade head
```

### 5. Создать platform_master пользователя (если еще не создан)

```bash
python -m app.db.autoseed
```

### 6. Запустить backend

```bash
python -m uvicorn app.main:app --reload
```

Backend будет доступен на: `http://localhost:8000`

### 7. Проверить работоспособность

```bash
curl http://localhost:8000/health
```

Или откройте в браузере:
- `http://localhost:8000/` - корневой endpoint
- `http://localhost:8000/health` - health check
- `http://localhost:8000/docs` - Swagger документация

---

## ФАЙЛЫ ДОКУМЕНТАЦИИ

- `SETUP_DATABASE.md` - Настройка базы данных
- `DEPLOYMENT.md` - Развертывание
- `FIXES_REPORT.md` - Отчет об исправлениях
- `FINAL_STATUS.md` - Финальный статус проекта

---

## TROUBLESHOOTING

### Ошибка подключения к БД

1. Проверьте, что PostgreSQL запущен
2. Проверьте `DATABASE_URL` в `.env`
3. Убедитесь, что база `modular_saas_core` существует

### Ошибка миграций

1. Убедитесь, что `psycopg2-binary` установлен
2. Проверьте `DATABASE_URL` в `.env`
3. Проверьте права доступа к БД

### Ошибка импорта модулей

1. Убедитесь, что виртуальное окружение активировано
2. Проверьте, что все зависимости установлены: `pip install -r requirements.txt`

---

**Готово к запуску!** 🚀





