# Правила копирования дизайна

## 📋 Основное правило

При переносе страниц или компонентов из другого проекта выполнять **чистую переноску дизайна**. Не копировать код частями — лучше переписать компонент заново, сохраняя внешний вид.

---

## ✅ ЧТО ПЕРЕНОСИТСЯ (только дизайн):

### 1. **Внешний вид интерфейса (дизайн)**
- Визуальный дизайн страницы
- Расположение элементов
- Цветовая схема
- Типографика

### 2. **Разметка и визуальная структура**
- HTML-структура (JSX)
- Layout компонентов
- Порядок элементов

### 3. **Стили и классы**
- Tailwind CSS классы
- Inline стили (если есть)
- CSS классы для визуальных эффектов
- Анимации и переходы

### 4. **UI-элементы и композиция**
- Кнопки, формы, карточки
- Композиция секций
- Отступы и расположение

---

## ❌ ЧТО НЕ ПЕРЕНОСИТСЯ (только дизайн):

### 1. **Бизнес-логика**
- ❌ Функции обработки данных
- ❌ Условная логика рендеринга
- ❌ Обработчики событий (логика)
- ❌ Валидация форм

### 2. **API-вызовы и загрузка данных**
- ❌ Fetch запросы
- ❌ API endpoints
- ❌ Способы загрузки данных
- ❌ Обработка ответов API

### 3. **State-менеджеры**
- ❌ Redux, Zustand, Context
- ❌ Глобальный state
- ❌ Сложные хуки для state

### 4. **Сервисы, хелперы, модели**
- ❌ Утилиты из старого проекта
- ❌ Модели данных
- ❌ Сервисные классы
- ❌ Кастомные хуки (кроме UI)

### 5. **Внутренние зависимости**
- ❌ Импорты из старого проекта
- ❌ Зависимости, которых нет в новом проекте
- ❌ Проприетарные компоненты

---

## 🔑 Ключевые требования:

### ✅ НЕ копировать код кусками
- Не копировать функции целиком
- Не копировать логику обработки
- Переписать компонент заново

### ✅ Если оригинал связан с логикой — верстать заново
- Извлечь только UI-часть
- Переписать на чистом React/Next.js
- Использовать структуру текущего проекта

### ✅ Импорты только из текущего проекта
- Использовать только существующие компоненты
- Использовать только существующие утилиты
- Не создавать временные зависимости

### ✅ Никаких зависимостей от старого проекта
- Полная изоляция от старого кода
- Компонент должен работать самостоятельно

---

## 📝 Работа с недостающими частями:

### Если компонент содержит функциональность:
1. **Выделить UI-часть** — только визуальные элементы
2. **Отбросить логику** — обработчики, вычисления, API
3. **Переписать заново** — с нуля на чистом коде

### Если нужны новые элементы:
1. **Создать с нуля** — новые компоненты/утилиты
2. **Адаптировать под проект** — использовать существующую структуру
3. **Не копировать из старого** — только дизайн вдохновляет

### Если функциональность не реализована:
1. **Поставить заглушку** — пустые функции или TODO
2. **Пометить TODO** — комментарий что нужно реализовать
3. **Не оставлять нерабочий код** — заглушка должна работать

---

## 📋 Формат результата:

### ✅ Должно быть:
- **Интерфейс выглядит как в исходном проекте** — визуально идентичен
- **Компоненты написаны с нуля** — новый чистый код
- **Адаптированы под структуру текущего проекта** — использует текущие паттерны
- **Код чистый и самостоятельный** — без зависимостей от старого проекта
- **Без неиспользуемых импортов** — только то, что нужно

### ❌ Не должно быть:
- Копий функций из старого проекта
- Импортов из старого проекта
- Бизнес-логики из старого проекта
- Неиспользуемого кода

---

## 🎯 Пример процесса копирования:

### Шаг 1: Анализ оригинала
```tsx
// ОРИГИНАЛ (старый проект)
export function OldComponent() {
  const [data, setData] = useState([])
  const { fetchData } = useOldHook()  // ❌ Старый хук
  
  useEffect(() => {
    fetchData().then(setData)  // ❌ Старый API
  }, [])
  
  return (
    <div className="bg-mishka">  // ✅ Дизайн - копируем
      <h1 className="text-4xl">  // ✅ Стили - копируем
        {data.title}  // ❌ Логика - не копируем
      </h1>
    </div>
  )
}
```

### Шаг 2: Создание нового компонента
```tsx
// НОВЫЙ КОМПОНЕНТ (текущий проект)
export function NewComponent() {
  // ✅ Только UI state (если нужен)
  const [loading, setLoading] = useState(true)
  
  // ✅ Используем новый API
  // TODO: Реализовать загрузку данных
  // const data = await loadData()
  
  return (
    <div className="bg-mishka">  // ✅ Дизайн скопирован
      <h1 className="text-4xl">  // ✅ Стили скопированы
        Заголовок  // ✅ Заглушка вместо логики
      </h1>
    </div>
  )
}
```

---

## 📚 Чеклист при копировании:

- [ ] Визуальный дизайн идентичен оригиналу
- [ ] Все стили (Tailwind классы) скопированы
- [ ] HTML-структура сохранена
- [ ] Убрана вся бизнес-логика
- [ ] Убраны API-вызовы из старого проекта
- [ ] Убраны импорты из старого проекта
- [ ] Используются только компоненты текущего проекта
- [ ] Код написан с нуля
- [ ] Нет неиспользуемых импортов
- [ ] Компонент работает самостоятельно
- [ ] Добавлены TODO для нереализованной функциональности

---

## 🚫 Запрещено:

1. ❌ Копировать функции целиком
2. ❌ Копировать обработчики событий с логикой
3. ❌ Использовать импорты из старого проекта
4. ❌ Оставлять зависимости от старого кода
5. ❌ Копировать утилиты и хелперы
6. ❌ Оставлять нерабочий код без заглушек

---

## ✅ Разрешено:

1. ✅ Копировать только CSS классы
2. ✅ Копировать только HTML-структуру
3. ✅ Переписывать компонент с нуля
4. ✅ Создавать заглушки для функциональности
5. ✅ Использовать только текущую структуру проекта
6. ✅ Адаптировать под паттерны текущего проекта




