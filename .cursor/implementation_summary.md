# Сводка реализации регистрации через Telegram/MAX

## ✅ Реализовано

### Backend (core-backend)

1. **VerificationService** (`app/services/verification.py`)
   - Генерация 6-значных кодов
   - Хранение в памяти (dev mode)
   - Логирование кодов в консоль
   - Проверка кодов с TTL 5 минут

2. **Endpoints в `/api/v1/auth`:**
   - `POST /request_code` - запрос кода для Telegram/MAX
   - `POST /confirm_code` - подтверждение кода и создание tenant_id
   - `POST /activate-module` - активация модуля (существующий)
   - `POST /dev-login` - dev-вход (существующий)

3. **Endpoints в `/api/v1/modules`:**
   - `POST /activate` - новая активация модуля с вызовом `/internal/register` и webhook

4. **Endpoints в `/api/v1/dev`:**
   - `POST /save-tenant-id` - сохранение tenant_id в .env.local (dev mode)

### Backend (modules/shop)

1. **Internal API** (`app/api/v1/internal/register.py`)
   - `POST /api/v1/internal/register` - регистрация tenant в модуле

2. **Webhooks** (`app/api/v1/webhooks/__init__.py`)
   - `POST /api/v1/webhooks/license.updated` - обработка обновления лицензии

### Frontend (core-frontend)

1. **Страница регистрации** (`app/register/page.tsx`)
   - Выбор метода (Telegram/MAX)
   - Ввод идентификатора
   - Ввод кода подтверждения
   - Автоматическое сохранение tenant_id в localStorage и .env.local

2. **API Routes:**
   - `/api/auth/request-code` - прокси для запроса кода
   - `/api/auth/confirm-code` - прокси для подтверждения кода
   - `/api/modules/activate` - прокси для активации модуля
   - `/api/dev/save-tenant-id` - сохранение tenant_id

3. **API Client** (`lib/api/register.ts`)
   - `requestCode()` - запрос кода
   - `confirmCode()` - подтверждение кода
   - `activateModuleNew()` - активация модуля (новый flow)

## 🔄 Flow регистрации

1. Пользователь выбирает Telegram или MAX
2. Вводит идентификатор (@username или max_id)
3. Получает код в консоль сервера (dev mode)
4. Вводит код
5. Создается tenant_id и сохраняется в localStorage и .env.local
6. Пользователь выбирает модуль и поддомен
7. Активируется модуль:
   - Вызывается `/internal/register` в модуле
   - Отправляется webhook `license.updated`

## ⚠️ Известные проблемы

1. Endpoints `/api/v1/auth/request_code` и `/api/v1/auth/confirm_code` могут не отображаться в OpenAPI, но должны работать
2. Для полного тестирования нужно запустить модуль shop на порту 8001

## 📝 Следующие шаги

1. Протестировать полный flow регистрации
2. Убедиться, что модуль shop запущен и доступен
3. Проверить работу webhook license.updated


