PROMPT: “CLEAN PAGE DESIGN EXTRACTOR”
(Для переноса дизайна страниц без мусора)


PROMPT: Copy Only Design Layouts (No Logic, No Trash)

Правила копирования.
копировать и перенести только дизайн страниц, сохранив визуальный вид 1-в-1, но полностью очистив структуру от:

❌ неиспользуемых компонентов
❌ старых шаблонов
❌ неактуальных стилей
❌ дублей файлов
❌ временных экспериментов
❌ старых хуков, логики, утилит
❌ мусорных директорий
❌ CSS, которые не влияют на дизайн
❌ layout’ов, которые не нужны в новой структуре

✔️ Что нужно сделать (ТОЧНОЕ ПОВЕДЕНИЕ)

Найди текущие фронтенд-страницы
(папки /pages/, /app/, /src/views/, /components/pages/)

Создай новую папку:

/clean_design/


Скопируй туда только дизайн каждого экрана, а именно:

JSX/TSX разметку

Tailwind классы

UI-компоненты

визуальную структуру

анимации (если есть)

стилевые файлы, реально влияющие на вид

НЕ копируй:

бизнес-логику

вызовы API

хуки

внутренние сервисы

контекст

состояние (useState/useEffect можно оставить только если связано с UI, но почистить логику)

Компоненты должны быть чистыми, как в Figma:

только визуальная часть

никакой логики

ничего, кроме дизайна

✔️ Как переписывать код
1. Убрать всё, что НЕ относится к UI:

❌ useEffect для загрузки данных
❌ fetch запросы
❌ сложные reduce/map логики
❌ Redux/Zustand
❌ сложные контейнеры

2. Оставить только UI:

✔ разметка
✔ структура блоков
✔ Tailwind
✔ UI-компоненты
✔ декларативная логика (модалки, ашкины)

3. Стандартизировать структуру каждого экрана:
/clean_design/
   /components/
   /pages/
   /layouts/
   /styles/

✔️ При копировании дизайна ты должен должен:
🟦 Упростить код:

вырезать все неиспользуемые пропсы

удалять логические ветки

соблюдать чистоту и минимализм

🟪 Очистить импорты:

удалять каждый неиспользуемый импорт

заменять кастомные UI-компоненты на стандартные, если можно

🟩 Сохранить визуал строго 1-в-1:

цвет

размеры

адаптив

сетку

анимации

📌Если я напишу :
"Перенеси весь дизайн проекта в /design-source, оставив только визуальные элементы и структуру страниц. Очисти каждую страницу от логики, API, состояния и мусора. Приведи весь UI к чистой, упрощённой и стандартизированной структуре."