# Когда можно работать с модулями

## ✅ РАЗРЕШЕНО работать с модулями:

### 1. **При разработке модуля**
- Создание новых файлов внутри модуля (`modules/shop/...`)
- Изменение логики модуля
- Добавление API endpoints в модуле
- Изменение UI модуля
- Создание миграций модуля

### 2. **При настройке интеграции модуля с ядром**
- Регистрация модуля в `registry.yaml`
- Настройка `manifest.json` модуля
- Добавление маршрутов в `app/[...slug]/page.tsx` (если нужно)
- Обновление SDK для модулей (если нужно)

### 3. **При исправлении ошибок модуля**
- Исправление багов в модуле
- Обновление зависимостей модуля
- Исправление типов TypeScript в модуле

### 4. **Когда модуль работает в runtime**
- Модуль доступен когда есть subdomain (например: `shop.localhost:7000`)
- Модуль загружается динамически через `dynamic import()`
- Модуль работает через SDK ядра

## ❌ НЕЛЬЗЯ работать с модулями:

### 1. **Если это нарушает структуру проекта**
- Нельзя создавать новые папки вне структуры модуля
- Нельзя переносить файлы модуля в core
- Нельзя менять архитектуру модуля без необходимости

### 2. **Если модуль не зарегистрирован**
- Нельзя использовать модуль, если он не в `registry.yaml`
- Нельзя работать с модулем без `manifest.json`

### 3. **Если работа с модулем ломает core**
- Нельзя делать прямые импорты из core в модуль (только через SDK)
- Нельзя изменять core ради модуля
- Нельзя добавлять зависимости модуля в core

## 🔍 Когда модуль доступен для работы:

### Runtime (когда модуль работает):
1. **Frontend**: Когда пользователь заходит на subdomain модуля
   - Пример: `shop.localhost:7000` → загружается shop модуль
   - Модуль загружается через `dynamic import()` в `app/[...slug]/page.tsx`

2. **Backend**: Когда запрос идет к модулю
   - Backend модуля запускается отдельно (порт 8001 для shop)
   - Модуль получает `tenant_id` из headers
   - Модуль использует SDK для работы с ядром

### Development (когда можно разрабатывать):
- ✅ Всегда можно изменять код модуля
- ✅ Можно создавать новые файлы в модуле
- ✅ Можно тестировать модуль локально
- ✅ Модуль изолирован от core при сборке

## 📋 Правила работы с модулями:

### 1. **Изоляция**
```
modules/
  shop/
    frontend/  ← Можно работать здесь
    backend/   ← Можно работать здесь
    manifest.json ← Можно обновлять
```

### 2. **Интеграция с ядром**
```
core-frontend/app/[...slug]/page.tsx  ← Можно добавлять роуты модуля
core-backend/app/modules/
  registry.yaml  ← Можно регистрировать модуль
  sdk.py         ← Можно расширять SDK (если нужно)
```

### 3. **SDK использование**
```python
# В модуле - ПРАВИЛЬНО
from app.modules.sdk import get_subscription_status

# В модуле - НЕПРАВИЛЬНО
from app.models.tenant import Tenant  # ❌ Прямой импорт из core
```

## ✅ Когда я (AI) могу работать с модулем:

1. **Пользователь просит изменить модуль** → ✅ Да, можно
2. **Нужно исправить ошибку в модуле** → ✅ Да, можно
3. **Нужно создать новый модуль** → ✅ Да, можно (следовать структуре)
4. **Нужно интегрировать модуль с ядром** → ✅ Да, можно (через SDK/registry)
5. **Нужно изменить core ради модуля** → ❌ Нет, нельзя (только если критично)

## 🎯 Вывод:

**Я могу работать с модулями когда:**
- ✅ Пользователь явно просит работать с модулем
- ✅ Нужно исправить/создать функционал модуля
- ✅ Модуль зарегистрирован и имеет `manifest.json`
- ✅ Изменения не нарушают структуру проекта
- ✅ Изменения не ломают core

**Модуль доступен для работы в runtime когда:**
- ✅ Пользователь заходит на subdomain модуля (`shop.localhost:7000`)
- ✅ Модуль зарегистрирован в `registry.yaml`
- ✅ Backend модуля запущен (если нужен)




