# Чеклист для локальной разработки

## ✅ Подготовка окружения

- [ ] Python 3.11+ установлен
- [ ] Node.js 18+ установлен
- [ ] PostgreSQL установлен и запущен (или Docker)
- [ ] Git установлен

## ✅ Настройка проекта

- [ ] Выполнен `.\setup-local-env.ps1` для создания .env файлов
- [ ] Проверены и отредактированы все .env файлы:
  - [ ] `core-backend/.env`
  - [ ] `core-frontend/.env.local`
  - [ ] `modules/shop/.env` (опционально)
  - [ ] `modules/shop/frontend/.env.local` (опционально)

## ✅ Установка зависимостей

### Core Backend
- [ ] Создано виртуальное окружение: `python -m venv venv`
- [ ] Активировано виртуальное окружение
- [ ] Установлены зависимости: `pip install -r requirements.txt`

### Core Frontend
- [ ] Установлены зависимости: `npm install`

### Shop Module (опционально)
- [ ] Backend: создано venv и установлены зависимости
- [ ] Frontend: установлены зависимости через npm

## ✅ База данных

- [ ] PostgreSQL запущен
- [ ] Создана база данных `tochkarosta_core`
- [ ] Выполнены миграции: `cd core-backend && alembic upgrade head`
- [ ] (Опционально) Создана база данных `shop_module` для модуля

## ✅ Запуск сервисов

### Вариант 1: Автоматический запуск
- [ ] Выполнен `.\start-local-dev.ps1`

### Вариант 2: Ручной запуск

#### Core Backend
```powershell
cd core-backend
.\venv\Scripts\Activate.ps1
uvicorn app.main:app --reload --port 8000
```

#### Core Frontend
```powershell
cd core-frontend
npm run dev:auth  # для порта 7001
# или
npm run dev       # для порта 7000
```

#### Shop Module (опционально)
```powershell
# Backend
cd modules/shop
python -m venv venv
.\venv\Scripts\Activate.ps1
uvicorn app.main:app --reload --port 8001

# Frontend
cd modules/shop/frontend
npm run dev
```

## ✅ Проверка работоспособности

- [ ] Core Backend доступен: http://localhost:8000/health
- [ ] Core Frontend доступен: http://localhost:7001
- [ ] Swagger документация доступна: http://localhost:8000/docs
- [ ] Страница регистрации работает: http://localhost:7001/register

## ✅ Dev-режим

- [ ] Dev-login работает: `POST http://localhost:8000/api/v1/auth/dev-login`
- [ ] Коды подтверждения выводятся в консоль сервера
- [ ] Mock-сервисы работают (SMS, verification)

## 🔧 Troubleshooting

### Backend не запускается
- Проверьте, что PostgreSQL запущен
- Проверьте `DATABASE_URL` в `.env`
- Проверьте, что порт 8000 свободен

### Frontend не запускается
- Проверьте, что Node.js установлен
- Проверьте, что порты 7000/7001/7002 свободны
- Проверьте `BACKEND_URL` в `.env.local`

### Ошибки подключения к БД
- Убедитесь, что PostgreSQL запущен
- Проверьте учетные данные в `DATABASE_URL`
- Проверьте, что база данных создана

### API routes возвращают 404
- Перезапустите Next.js dev-сервер
- Проверьте, что backend запущен
- Проверьте `BACKEND_URL` в `.env.local`

## 📝 Полезные команды

### Остановка всех сервисов
```powershell
Get-Job | Stop-Job
Get-Job | Remove-Job
```

### Просмотр логов
```powershell
Get-Job | Receive-Job
```

### Проверка портов
```powershell
netstat -ano | findstr ":8000"
netstat -ano | findstr ":7001"
```


